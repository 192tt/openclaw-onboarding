version: "3.8"

services:
  openclaw-platform:
    build: .
    container_name: openclaw-test
    ports:
      - "8000:8000"
    volumes:
      - openclaw-data:/app/data
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  openclaw-data:
